# 작업일지 2026-08-17~18 — 8월 남한에서 한반도로, external DEM 재설계

대상이 **남한 8월 → 한반도 8월**로 넓어진 날. 범위를 넓히자 조용히 있던 전제가
셋 드러났다.

1. **external DEM이 남한 전용이었다** — 북위 39.9°까지라 북한이 통째로 빠진다.
2. **STAC 요약에 footprint가 없었다** — 남한 판정 도구가 늘 "0장"을 돌려주고 있었다.
3. **KST 환산을 안 하고 있었다** — 하강궤도는 UTC 21시대라 KST로 날짜가 하루 넘어간다.

---

## 1. 8월 수집 — 실제로 부족한 건 1장뿐이었다

CDSE 관측 목록과 로컬·NAS·VH RTC 보유분을 대조했다. 키는 **관측 시작시각 +
절대궤도**(ISSUES #16).

| 시기 | VH완료 | 로컬·미처리 | 받아야함 |
| --- | ---: | ---: | ---: |
| 2025-08 (남한) | 10 | 29 | 0 |
| 2026-08 (남한) | 6 | 11 | **1** |

받은 것은 `B5F3`(2026-08-08, 491 MB) 하나다. "다운로드가 밀렸다"고 생각했는데
실제로는 **원본은 거의 다 있고 RTC가 밀려 있었다.**

## 2. 🐛 STAC 요약에 footprint가 빠져 있었다

`download_south_korea_month.py`가 검색 66장을 전부 훑고도 **"남한 걸침 0장"**
을 냈다. 원인은 이 도구가 아니라 그 아래였다.

```python
# s1/stac/models.py — S1ItemSummary 에 geometry 필드가 없었다
bbox: Optional[List[float]]      # ← bbox 는 있는데
# geometry 가 없다 → c.get("geometry") 가 항상 None → 판정 스킵
```

`extract_s1_summary()`가 `item.geometry`를 버리고 있어서, 요약 dict를 받아
footprint로 판정하는 쪽은 **무조건 빈 결과**를 봤다. bbox만으로 판정하면 기울어진
스와스의 빈 삼각형까지 육지로 세는 문제(SCENE_FOOTPRINT_REAUDIT) 때문에 일부러
footprint를 쓰게 해놨는데, 정작 그 데이터가 전달되지 않았다.

**조치** — `S1ItemSummary.geometry` 추가 + `extract_s1_summary`에서 채운다.
고친 뒤 같은 명령이 **남한 걸침 57장**을 정상 판정했다(ISSUES #18).

## 3. 26년 7·8월 관측 달력 문서화

[ORBIT_CALENDAR_202607_08_KR.md](../pipeline/ORBIT_CALENDAR_202607_08_KR.md) 신규.
날짜 × 상대궤도, 상대궤도 × 대권역 커버율, 실제 반복 주기를 정리했다.

- 한반도를 지나는 궤도는 **8개**(25·32·54·61·105·127·134·156). 이 중 **남한을
  찍는 것은 5개**(32·54·61·127·134)뿐이다.
- **제주는 rel 134만** 찍는다. **동부(형산강·태화강·회야수영·낙동강동해)는
  54·61만** 찍는다. 궤도는 서로 대체재가 아니다.
- 12일 주기가 **실제로는 안 지켜진다** — rel 134의 08-01, rel 61의 07-15가 비었다.
  S1A 퇴역(2026-06-29) 직후 관측계획 재편 중이라 그렇다.

이 달력을 근거로 [BASELINE_DESIGN_KR.md](../drought/BASELINE_DESIGN_KR.md)에
**1-1. 상대궤도별 3층 적용**을 붙였다. 궤도마다 병목이 다르다는 것이 요지다 —
rel 127은 셋 다 막힌 데가 없고, rel 54는 층 3(`dry` 표본 1일), rel 61·32는
층 2(연도 표본)에서 막힌다.

## 4. ⚠ KST 환산 — 하강궤도는 날짜가 하루 넘어간다

| UTC | KST | 궤도 |
| --- | --- | --- |
| `D`일 09:16~09:40 | 같은 날 18시대 (저녁) | 25 · 54 · 127 · 156 (상승) |
| `D`일 21:21~21:47 | **`D+1`일** 06시대 (아침) | 32 · 61 · 105 · 134 (하강) |

카탈로그의 `2026-08-13` rel 134는 실제로 **KST 8/14 06:30** 관측이다. 강우·사건
자료와 맞출 때 이걸 놓치면 **사건 전 영상을 사건 후로 착각**한다. 처음 쓴 달력에
이 환산이 빠져 있어 바로 고쳤다.

## 5. external DEM이 남한 전용이었다 — 한반도판을 새로 구웠다

### 발단

"한반도 8월 전량 전처리"로 범위가 넓어지자 DEM이 걸렸다.

```text
korea_full_cop30.tif        125.0~131.0E, 32.9~39.9N     ← 남한 + 제주 전용
북한 최북단                                    약 43.0N     ← 3.1° 밖
```

미처리 씬의 footprint를 실측하니 북위 **43.57°**, 서경 **122.03°**까지 나왔다.
DEM 밖은 RTC 산출물에서 무효로 남으므로, 그대로 돌리면 **북한이 통째로 빈
산출물**이 나온다. 오류 없이 결과만 비는 종류라 로그로는 안 보인다(ISSUES #13의
확대판 → #17로 등록).

### 조치

```bash
python -m s1.tools.dem.make_basin_dem --bounds 123.5,32.8,131.3,43.3 --name korea_peninsula
# korea_peninsula_cop30.tif  123.50~131.30E 32.80~43.30N  타일 78  1,713 MB
```

COP30 타일 가용성을 먼저 확인했다(E123~131 × N32~43). 빠진 칸은 전부 바다라
문제없고, **한반도 육지 타일은 전부 있다.**

같은 날 [PREPROCESSING_SPEC_KR.md](../pipeline/PREPROCESSING_SPEC_KR.md)가
신설되면서 **한반도 처리 DEM은 `korea_peninsula_cop30.tif` 하나로 확정**됐다.
보유 DEM 8종의 범위를 실측한 표와 나머지 전처리 파라미터(편파 VH·Frost·EGM
끄기·10 m·EPSG:4326)까지 그 문서가 정본이다.

### 기존 산출물은 재처리가 필요한가 — 재지 않고 답하지 않았다

DEM을 바꿨으니 기존 것도 다시 구워야 하나? **씬마다 다르다.** 그래서 가정하지
않고 `(footprint ∩ 한반도)` 중 옛 DEM 상자 **밖** 비율을 씬별로 계산했다.

| 판정 | 씬 | 조치 |
| --- | ---: | --- |
| 손실 < 1% | 15 | 그대로 둔다. 이 중 11장이 이미 VH 완료 |
| 손실 ≥ 1% | 17 | peninsula DEM 으로 처리 — **전부 미처리분이었다** |

**이미 처리한 11장은 하나도 재처리 대상이 아니었다**(대부분 0.0%). 남한 궤도의
프레임이라 옛 DEM 안에 온전히 들어간다. 재처리 11장(약 5시간)을 아꼈고, 무엇보다
"DEM을 바꿨으니 다 다시"라는 반사적 판단을 숫자로 걸렀다.

반대로 손실 100%짜리가 7장 있었다 — `226A`·`31FB`·`E6FC`·`65AB`·`9A5C`·`B808`·
`A54D`·`7AE5`. 전부 북한·동해 프레임이다.

### ⚠ 강제 종료가 빈 산출물을 남겼다 — 확인을 잘못했다

배치를 죽인 뒤 "부분 산출물 없음"으로 판단했는데 **틀렸다.** 그때 실제로 처리
중이던 씬은 `D635`(배치 B)였는데, 엉뚱하게 `0D06`·`5ACA`(새 배치가 곧 처리할
씬)를 찾아보고 깨끗하다고 결론냈다. `D635`는 **0.15 GB짜리 값이 전부 0인
껍데기**로 남았다.

이게 고약한 이유는 세 가지다.

1. **열린다.** `rasterio`로 크기·bounds·CRS가 정상으로 보이고, 성기게 읽으면
   유효화소 100%로 나온다. README가 경고한 "쓰다 만 파일도 열린다"가 이것이다.
2. **배치가 건너뛴다.** 파일이 있으면 처리 완료로 보므로 재실행해도 안 고쳐진다.
3. **하필 제일 중요한 씬이었다.** `D635`는 08-13 rel 134 패스에서 **남한 커버가
   가장 넓은(85.6%)** 프레임이다.

**판정법** — 크기만으로는 애매하다(정상 산출물도 1.6~2.9 GB로 폭이 넓다).
**압축률**이 갈랐다.

| 씬 | 용량 | 무압축 대비 | 비영값 | 판정 |
| --- | ---: | ---: | ---: | --- |
| 정상 19장 | 1.72~2.85 GB | 51.8~87.4% | 100% | OK |
| **`D635`** | **0.15 GB** | **5.1%** | **0.0%** | ❌ 빈 파일 |

값이 전부 0이면 deflate가 극단적으로 잘 먹어 **압축률이 한 자릿수**로 떨어진다.
20장 전수 점검 결과 문제는 `D635` 하나뿐이었고, 삭제 후 재처리 대기열에 넣었다.

> **규칙**: 배치를 강제 종료했으면 **그 시점 temp 폴더에 있던 씬**의 산출물을
> 지운다. 그리고 "부분 산출물 없음"을 눈으로 확인하지 말고 **압축률·비영값으로
> 전수 점검**한다.

### 진행 중이던 배치는 중단했다

남한 11장 배치를 돌리는 중이었는데, 남은 6장 중 **3장(`0868` 2.4% · `D2A0` 2.7%
· `7C24` 17.9%)이 손실 대상**이었다. 그대로 끝내면 어차피 재처리라 중단하고
peninsula DEM 으로 다시 걸었다. 부분 산출물이 남지 않은 것과 완료본 11장이
온전한 것은 확인했다(강제 종료 후 점검 — README 주의사항).

## 6. 2026년 8월 한반도 처리 현황

| 구분 | 씬 | DEM | 상태 |
| --- | ---: | --- | --- |
| 한반도 교집합 ≥ 1% | **31** | — | 처리 대상 |
| ├ 완료 | 11 | `korea_full_cop30.tif` | ✅ (손실 <1% 실측 확인) |
| └ 진행 | 20 | `korea_peninsula_cop30.tif` | 🔄 배치 2개 병렬, 08-18 01:27 시작 |
| 한반도 교집합 < 1% | 2 | — | ⏸ 제외 |

제외한 2장은 `B5F3`(0.00%, 로컬 보유)와 `8B48`(0.39%, 미보유 — 러시아·동해).
`download_korea_missing.py`가 이미 쓰는 1% 규칙과 같은 기준이다.

## 6-2. 감시 자동화 + 상시 현황 창

**감시를 작업 스케줄러에 올렸다** — `S1_new_scene_monitor`, 1시간 간격
([SCENE_MONITOR_KR.md](../pipeline/SCENE_MONITOR_KR.md) 3-A). 등록 후 첫 실행
결과 0(정상). 7월 22일 이후 멈춰 있던 감시가 다시 돈다.

그런데 감시는 **"새 게 올라왔다"까지**다. 알림을 놓치면 끝이고, 받은 뒤의
진행상황(대기·처리중·완료)은 아무 데도 안 보인다. 그래서 회사 PC에 띄워 둘
작은 창을 만들었다 — [SCENE_DASHBOARD_KR.md](../pipeline/SCENE_DASHBOARD_KR.md).

| 표 | 내용 |
| --- | --- |
| 최근 촬영 | 촬영 하나당 한 줄 — 촬영시각(KST) · 씬 · 상대궤도/상승하강 · **위치(충남·전남 …)** · 한반도 겹침% · 원본 크기 · 상태(미수신→받는중→대기→전처리중→완료) |
| 전처리 | 처리중(경과·기록된 GB) · 대기 · 완료 |

카탈로그와 다운로드를 표 둘로 나눴다가 **하나로 합쳤다** — 같은 촬영이 양쪽에
나와 눈이 두 번 갔다. 둘을 잇는 것은 어차피 촬영시각이라 한 줄로 묶고 `상태`
칸 하나로 어디까지 왔는지를 말한다.

### 🐛 "tif가 있으면 완료"가 틀렸다

SNAP `gpt`는 출력 tif를 **처리하는 내내 쓴다.** 만들면서 확인해 보니 처리 중이던
`0868`이 이미 2.32 GB짜리 tif를 갖고 있었고, 방금 시작한 `8E47`은 0 바이트였다.
파일 존재로 완료를 세면 **굽는 중인 씬이 완료로 잡힌다.** 그래서 판정을
**배치 러너의 임시폴더(`%TEMP%/frostrtc_*`) 우선**으로 뒤집었다 — 그 폴더가 있는
동안이 곧 처리 중이다.

경과시간도 한 번 틀렸다. 임시폴더 안 zip의 mtime을 쟀더니 "20시간 전 시작"이
나왔는데, `shutil.copy2`가 원본 시각을 그대로 물려주기 때문이다. **폴더
생성시각**으로 바꿔 실제 배치 시작(08:57·10:11)과 맞췄다.

### 촬영 계획도 붙였다 — exe 262 MB 를 300줄로

`SentinelObservationPlan/`에 같은 일을 하는 **PyInstaller exe(262 MB, 소스 없음,
서명 없음)**가 들어와 있었다. 감사도 수정도 안 되고, ESA가 KML 형식을 바꾸면
손댈 방법이 없다. 게다가 `.gitignore`에 걸리지 않아 `git add -A` 한 번이면
커밋될 뻔했다(GitHub 파일당 100 MB 한도).

저장소에는 이미 부품이 있었다 — 순수 파이썬 point-in-polygon, 시도 경계,
한반도 경계. [acquisition_plan.py](../../s1/tools/monitor/acquisition_plan.py)로
다시 썼다(표준 라이브러리 300줄, [문서](../pipeline/ACQUISITION_PLAN_KR.md)).

```text
■ 앞으로 10일 한반도 촬영 계획 — 6건
  08-19 18:30 KST  S1D rel127  전남·전북·경남      한반도 15.5%  댐·보 16곳
  08-26 06:24 KST  S1C rel134  함경남도·함경북도·강원 한반도 69.5%  댐·보 42곳
```

- **육지 격자를 미리 만든다.** 계획의 datatake는 위도 10°를 넘는 긴 띠라
  프레임처럼 건건이 격자를 뿌리면 35건에 50초가 걸린다. 한반도 육지 점을 한 번만
  만들어 두고 "그중 몇 개가 드나"만 세니 0.9초다. 숫자의 뜻도 분명해졌다 —
  **한반도 육지의 몇 %를 덮나**.
- 검증: 계획에 잡힌 상대궤도가 25·61·105·127·134·156으로 **전부 관측 달력의
  한반도 통과 궤도 8개 안**에 들었고, rel134가 69.5%로 가장 넓다(남북 종단 패스).
- 댐·보 51곳(`data/dam_points_kwater.csv`)은 exe 옆에 있던 CSV 3개를 합친 것이다.
  시도 경계에 찍어 검증했다 — 육지 밖·중복 0건.

### 위치는 어떻게 "충남"이 되나

시도 경계를 **500 m로 단순화한 GeoJSON 한 장**(28개 구역, 326 KB)으로 구워 두고
([build_sido_geojson.py](../../s1/tools/monitor/build_sido_geojson.py)), footprint
표본 격자점이 어느 시도에 떨어지는지 세어 육지 표본 8% 이상만 적는다.
남한 17개는 읍면동 경계를 시도로 병합, 북한 11개는 WFP COD-AB adm1이다.
겹침%와 위치를 **같은 격자 한 번**으로 구해 프레임당 비용을 두 배로 만들지 않았다.

검증: 하강궤도 rel134 한 패스가 `제주 → 전남·경남·전북 → 경북·충남·충북 →
강원·경기·강원도(북) → 함경남도·양강도·함경북도`로 남→북 순서로 찍혔다.

### 🐛 "왜 8/14·8/8이 아직 대기지?" — 답이 둘로 갈렸다

현황 창을 보다 나온 질문이다. 답은 **씬마다 달랐고, 화면은 둘을 구별하지 못하고
있었다.**

| 씬 | 촬영(KST) | 한반도 겹침 | 실제 사정 |
| --- | --- | ---: | --- |
| `D635` | 2026-08-14 06:31 | **84.91%** | 진짜 밀렸다. 강제 종료로 껍데기가 남아 지웠는데(5절), 그 뒤 돌린 배치 두 개의 `--only` 목록에 **이 씬이 안 들어갔다** |
| `B5F3` | 2026-08-08 18:22 | **0.00%** | 남해 위 프레임. 애초에 처리 대상이 아니다(6절에서 제외로 분류한 그 씬) |

겹침은 **원본 zip 안 `preview/map-overlay.kml`**로 쟀다. 카탈로그 조회창(7일)을
벗어난 씬은 STAC geometry를 다시 받을 수 없어서다.

**조치** — 상태에 `제외`를 신설했다. 보유 zip 중 겹침 1% 미만은 대기가 아니라
제외로 찍고, 결과는 `downloads/footprint_cache.json`에 캐시한다. 이 구분이 없으면
중국·일본·동해 프레임이 영원히 "대기"로 남아 진짜 일감과 섞인다(그날 5건이 그랬다).
`D635`는 그 뒤 재처리를 걸어 완료됐다.

## 7. 오늘 밟은 함정

| 함정 | 증상 | 조치 |
| --- | --- | --- |
| STAC 요약에 footprint 누락 | 남한 판정이 늘 0장 | `S1ItemSummary.geometry` 추가 (#18) |
| DEM이 남한 전용 | 북한이 통째로 무효가 될 뻔 | peninsula DEM 신규 (#17) |
| KST 환산 누락 | 하강궤도 날짜가 하루 어긋남 | 달력에 환산표 |
| `conda run -c "여러 줄"` | `AssertionError: newlines` | 환경 python.exe 직접 호출 또는 파일로 |
| 환경마다 패키지가 다름 | `sar-gee`에 dotenv 없음 / `s1_pipeline`에 pyogrio 없음 | 대권역 union을 GeoJSON으로 내보내 환경 간 전달 |
| 배치 stdout 버퍼링 | 로그에 진행이 안 보임 | 산출물·프로세스·temp 폴더로 진행 확인 |
| 강제 종료 후 확인 대상 착오 | `D635`가 값 0인 껍데기로 남음 | 압축률·비영값 전수 점검(5절) |
| 처리 시간 과소 추정 | "씬당 30분" → 실제 **71~128분** | 원인은 큰 DEM이 아니었다 — 아래 참고 |
| tif 존재 = 완료로 착각 | 굽는 중인 씬이 완료로 집계 | 임시폴더 우선 판정(6-2) |
| `copy2`가 mtime을 물려줌 | 처리 경과가 "20시간 전 시작" | 임시폴더 생성시각으로 측정(6-2) |
| 배치 `--only` 목록에서 누락 | `D635`(한반도 85%)가 조용히 대기로 남음 | 상태에 `제외` 신설 — 겹침 미달과 진짜 밀린 것을 가른다(6-2) |
| Treeview 스크롤 기억 | 예정 줄이 뒤늦게 붙자 6건 중 2건만 보임 | 맨 윗줄이 바뀌면 `yview_moveto(0)` |
| POEORB 미공개 → RESORB 대체 | 8월분 18씬 **전량**이 정밀궤도 없이 처리됨. `continueOnFail=false`가 못 막는다 | 규격 2-2절에 기록, 재처리 여부는 실측 후 결정 |
| `D635` 재처리가 큐에 안 들어감 | 삭제만 하고 배치 목록에 없어 **이대로 끝나면 계속 없음** | 아래 8절 |

### 처리 시간 — 씬당 30분이 아니라 100분대였다 (원인은 DEM이 아니다)

배치가 끝나갈 무렵 로그를 DEM 세대별로 모아 다시 쟀더니, 처음에 적었던
"큰 DEM + 북부 대형 프레임" 설명이 **숫자와 맞지 않았다.** 전부 2병렬 조건이다.

| DEM | 용량 | n | 평균 | 범위 |
| --- | ---: | ---: | ---: | --- |
| 자동 캐시 COP30 | — | 17 | **58.2분** | 19.9~106.8 |
| `korea_cop30` | 0.50 GB | 5 | 86.0분 | 70.1~102.2 |
| `korea_full` | 0.73 GB | 5 | 90.4분 | 77.6~105.0 |
| **`korea_peninsula`** | **1.71 GB** | 20 | **100.9분** | 70.7~128.4 |

- **DEM 용량은 거의 무관하다.** 파일이 2.34배(화소수 1.95배) 커졌는데 시간은
  **+11.6%**뿐이다. SNAP은 DEM 전체가 아니라 **씬에 걸치는 타일만** 읽는다.
- **진짜 계단은 08-11의 자동 DEM → external DEM**(58.2 → 90.4분, +55%)이었다.
  자동 DEM이 빨랐던 건 성능이 아니라 **산출물 대부분이 결측이라 쓸 화소가
  없었기 때문**이다. 그 표본에는 9.6·8.4·14.6분짜리 **깨진 산출물**이 섞여 있다.
  즉 **"씬당 30분"은 애초에 고장난 결과에서 나온 하한값**이었다.
- **"북부 대형 프레임"도 틀렸다.** 입력 zip 평균은 2026-07 **1,068 MB** vs
  2026-08 **1,045 MB**로 8월이 오히려 작다. 완료된 20씬에서 zip 크기와 시간의
  상관은 **r = 0.49(R² 0.24)**, +100 MB당 **+4.4분**에 그친다.

gpt 메모리는 4~6 GB에서 **9~11 GB**로 올랐다. 앞으로의 일정 추정은
**씬당 100분 / 2병렬 = 시간당 1.2씬**을 쓴다. 근거표는
[PREPROCESSING_SPEC_KR.md](../pipeline/PREPROCESSING_SPEC_KR.md) 2-1절.

**배치 2개 병렬이 RAM 상한**이다(32 GB 중 여유 0.4 GB). 세 번째 배치를 띄우면
스와핑이 나므로, 추가 작업은 앞 배치가 끝난 뒤로 큐를 건다.

## 8. 이어서 할 일

> ### 🔴 `D635`가 실행 중인 배치 어디에도 없다 (08-18 17:30 확인)
>
> 껍데기를 지우고 "재처리 대기열에 넣었다"고 적었지만, **실제로 도는 배치
> 두 개(`_kp_c` 10씬 · `_kp_d` 10씬)의 목록에 `D635`가 없다.** 이대로 두면
> 배치가 끝나도 산출물은 30/32에서 멈춘다(`B5F3`는 정상 제외).
> 원본 zip은 `downloads/sentinel1_grd/...D635_COG.zip`(1.20 GB)로 남아 있다.
> **두 배치가 끝난 뒤 `D635` 단독으로 걸 것.** 08-13 rel 134에서 남한 커버가
> 가장 넓은(85.6%) 프레임이라 빠지면 그 패스가 반쪽이 된다.

- **`D635` 단독 재처리** (위 상자) — 최우선.
- **2025-08 한반도** — 미처리 44장쯤. 8월 연도 간 비교의 나머지 절반이다.
  특히 **rel 134의 25-08-11·08-23**이 26-08-13(KST 8/14)과 위성까지 같은 짝인데
  아직 미처리다.
- **7월분·25년분 68씬의 북부 결측 실측** — 8월분과 같은 방법으로 재고, 1% 이상만
  재처리한다(TODO P0.5b). 남한 궤도 위주라 대부분 면제될 것으로 보이지만
  **재기 전에는 단정하지 않는다.**
- 강우 자료 연계 — 층 3의 `dry`/`wet`/`flood` 태그가 지금은 사건 기준 잠정값이다.

## 관련 문서

- [PREPROCESSING_SPEC_KR.md](../pipeline/PREPROCESSING_SPEC_KR.md) — **전처리 규격
  정본**(DEM 선택 근거, 그래프 8단계, 확정 파라미터)
- [ORBIT_CALENDAR_202607_08_KR.md](../pipeline/ORBIT_CALENDAR_202607_08_KR.md) —
  26년 7·8월 관측 달력, KST 환산표
- [BASELINE_DESIGN_KR.md](../drought/BASELINE_DESIGN_KR.md) — 3층 설계와 궤도별 적용
- [ISSUES_KR.md](ISSUES_KR.md) — #17(DEM 범위) · #18(footprint 누락)
- [TODO_KR.md](TODO_KR.md) — P0.5b(결측 실측) · P0.6(8월 한반도)
